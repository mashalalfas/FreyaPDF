# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Workspace artifacts
.openclaw/
.understand-anything/
NAVIGATION_TREE.md
graphify-out/

# NOTE: `legal/` and `legal-config.json` are intentionally TRACKED legal & OSS-
# compliance artifacts (EULA, PRIVACY policy, SBOM, allowlist/blocklist config).
# They are NOT ignored so the SBOM/license gate and legal docs ship with VCS.

# Local / tooling artifacts
Telegram Chat History/
.graphify_*

# Pipeline / hero-stage local outputs (regenerated by build stages; not VCS)
archive/
memory/
android/build/
knowledge/

# Secrets
key.properties
*.jks
*.keystore

# Agent files
AGENTS.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md

# HERO sandbox state mirror
.hero/

# Old APK backups — too large for git, kept locally
backups/
